export default function Company({name, catchPhrase, bs}) {
  return (
    <div>

    <div>
        <h1>Company:</h1>
        <h3>Name: {name}</h3>
        <h3>Catch-Phrase: {catchPhrase}</h3>
        <h3>BS: {bs}</h3>
    </div>
        <hr/>
    </div>

  );
}